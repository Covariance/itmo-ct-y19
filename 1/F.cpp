#include <bits/stdc++.h>

using namespace std;

bool checkZhig(vector<int> vals, vector<bool> poly) {
    int sum = 0;
    for (int i = 0; i < poly.size(); i++) {

    }
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < (1 << n); i++) {
        i++;
    }
    return 0;
}
